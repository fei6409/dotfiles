#!/usr/bin/env bash

BASE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
TARGET_PATH="$("$BASE_DIR"/zoxide-select-path)"

# Navigate path in new window in current herdr session
[[ -n "$TARGET_PATH" ]] && herdr tab create --focus --cwd "$TARGET_PATH"
